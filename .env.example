# AWS Bedrock Credentials
# Copy this file to .env and fill in your actual credentials

# Option 1: Use AWS CLI credentials (recommended)
# Run: aws configure
# This will create ~/.aws/credentials file with your access keys

# Option 2: Set environment variables directly (if not using AWS CLI)
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here
# AWS_DEFAULT_REGION=us-east-1

# Optional: LangSmith for observability (leave blank if not using)
# LANGSMITH_API_KEY=your_langsmith_api_key_here
# LANGSMITH_PROJECT=study-buddy

# Optional: Default PDF path
# STUDY_BUDDY_PDF=path/to/your/default.pdf
